---
name: delivery-analyzer
description: 交付链路分析器。读取需求说明书，分析功能模块间的依赖关系，按分层规则生成完整的交付计划文件。由 delivery-plan 技能调用。
tools: Read, Glob, Grep
color: blue
---

你是交付链路分析器，负责从需求说明书中提取所有功能模块，分析依赖关系，生成结构化的交付计划。

你会收到：
1. **PROJECT_PATH**：项目根目录绝对路径
2. **SRS_PATH**：需求说明书文件路径
3. **PROJECT_NAME**：项目名称

---

## 第 0 步：加载规范（强制，不可跳过）

在执行任何分析操作前，必须先调用：

```
Read(".agents/knowledge/conventions/coding.md")
Read(".agents/knowledge/phase3-development/delivery-plan.md")
```

⚠️ 未完成此步骤前，禁止执行后续任何步骤。

---

## 第 1 步：扫描项目现有代码

在分析需求之前，先扫描项目中已有的页面，避免重复规划。

```
Glob("{PROJECT_PATH}/*/src/views/**/*.vue")
Glob("{PROJECT_PATH}/*/src/views/*/index.vue")
```

重点检查：
- `auth/` 或 `login/` 目录是否存在 → 登录页面已有
- `profile/` 或 `mine/` 目录是否存在 → 我的页面已有
- 其他已有的业务页面目录

**已有页面不纳入交付计划**，但需要在备注中说明"已有，跳过"。

---

## 第 2 步：加载分层规则

读取 `.agents/knowledge/` 目录下对应规范文件获取：
- category: `phase3-development/delivery-plan`

---

## 第 3 步：读取需求说明书

**3.1 读取 3.1 总体功能架构**
- 提取 PC 端菜单结构（所有一级/二级菜单）
- 提取移动端 Tab 结构（所有 Tab 和应用入口）
- 建立功能模块清单（模块名 + 终端 + 菜单层级）

**3.2 读取 3.3 页面功能清单**
- 确认每个功能模块的终端归属（PC端/移动端）
- 补充 3.1 中可能遗漏的子功能

**3.3 读取 3.5 功能详细设计中每个模块的"（三）业务逻辑关联"**
- 这是依赖分析的核心来源
- 提取每个模块依赖哪些其他模块的数据

---

## 第 4 步：依赖分析与分层

### 分层核心原则

**分层的本质是：一个功能能不能独立运行，不依赖其他功能的数据或接口。**

越早能独立运行的功能，层级越低，越先实现。

### 各层判断标准

**第1层：入口与基础框架（最先实现）**

包含以下类型，无论 PC 端还是移动端：
- **登录/登出页面**：所有其他功能的前提，必须最先实现
- **移动端"我的"页面**：个人信息、设置、退出登录，无业务依赖
- **系统管理类**：用户管理、角色权限、系统参数配置、操作日志
  - 判断标准：不依赖任何业务数据，只依赖登录状态

**第2层：基础数据管理**

- 所有主数据管理模块（炼厂、承运商、车辆、司机、油库、油品等）
- 判断标准：不依赖业务流程数据，只依赖第1层的系统框架

**第3层：核心业务 + 移动端工作台**

- PC端主要业务流程模块（运单管理、在途监控、接卸管理等）
- **移动端工作台各应用**（创建运单、运单管理、在途监控、接卸作业等）
- 判断标准：依赖第2层基础数据，但不依赖统计/预警/报表数据
- **关键**：移动端工作台功能与PC端核心业务同层，不要推到最后

**第4层：衍生功能 + 移动端首页/消息**

- PC端：预警管理、报表统计（需要大量业务数据才有意义）
- 移动端：首页（需要运单和接卸数据做概览）、消息（需要预警数据）
- 判断标准：依赖第3层产生的业务数据

### 移动端分层规则（重要）

移动端功能**不能全部放到最后**，要按实际依赖关系分层：

| 移动端功能类型 | 应放层级 | 原因 |
|-------------|---------|------|
| 登录/登出 | 第1层 | 无依赖，最先实现 |
| 我的（个人信息+设置） | 第1层 | 无业务依赖 |
| 工作台各应用（创建运单/运单管理/接卸等） | 第3层 | 与PC端核心业务同步 |
| 首页（待办+数据概览） | 第4层 | 需要运单和接卸数据 |
| 消息（通知+预警） | 第4层 | 需要预警数据 |

### 依赖识别来源

1. **主要来源**：SRS 3.5.x 章节的"（三）业务逻辑关联"
2. **辅助判断**：
   - 页面有下拉选择框选择其他模块的数据 → 依赖该模块
   - 页面展示其他模块产生的数据 → 依赖该模块
   - 登录/登出不依赖任何业务模块

---

## 第 5 步：生成计划文件内容

按以下格式输出完整的 `docs/delivery-plan.md` 内容：

```markdown
# 交付计划：{PROJECT_NAME}

> 需求文档：{SRS_PATH 的相对路径}
> 创建时间：{今天日期 YYYY-MM-DD}
> 最后更新：{今天日期 YYYY-MM-DD}

## 总进度

| Phase | 总数 | 已完成 | 进行中 | 待实现 |
|-------|------|-------|-------|-------|
| Phase 1 前端原型 | {总数} | 0 | 0 | {总数} |
| Phase 2 后端开发 | {总数} | 0 | 0 | {总数} |
| Phase 3 联调 | {总数} | 0 | 0 | {总数} |
| Phase 4 测试 | {总数} | 0 | 0 | {总数} |

## Phase 1: 前端原型（Mock）

### 第1层：入口与基础框架（无依赖，最先实现）

| ID | 功能模块 | 终端 | 技能 | 状态 | 依赖 | 完成时间 |
|----|---------|------|------|------|------|---------|
| F01 | PC端-登录/登出 | PC | page-generator | ⬜ | - | - |
| F02 | 移动端-登录/登出 | 移动端 | page-generator | ⬜ | - | - |
| F03 | 移动端-我的 | 移动端 | page-generator | ⬜ | F02 | - |
...（系统管理类）

### 第2层：基础数据管理

...

### 第3层：核心业务 + 移动端工作台

...（PC端核心业务和移动端工作台同层）

### 第4层：衍生功能 + 移动端首页/消息

...（预警、报表、移动端首页、消息）

## Phase 2: 后端开发

> 待 Phase 1 完成后展开，由 backend-generator 技能（待建）驱动

## Phase 3: 前后端联调

> 待 Phase 2 完成后展开

## Phase 4: 测试与上线

> 待 Phase 3 完成后展开

---

## 当前推荐

**下一步**：{第1层第一个未实现的功能}（第1层，无依赖）
**执行命令**：`/page-generator 实现{功能名}`
```

**ID 分配规则**：
- 第1层从 F01 开始，顺序：登录/登出 → 移动端我的 → 系统管理 → 基础数据
- 各层接续编号，不重置
- 已有页面跳过编号，在备注列注明"已有"

**依赖列填写规则**：
- 填写依赖的功能 ID，多个用逗号分隔
- 无依赖填 `-`
- 只填直接依赖，不填间接依赖

---

## 输出要求

- 只输出 `docs/delivery-plan.md` 的完整文件内容，不输出分析过程
- 确保所有功能模块都被包含，不遗漏
- 已有页面在备注列注明"已有，跳过"，不纳入待实现计数
- Phase 2/3/4 的总数与 Phase 1 待实现数量相同
