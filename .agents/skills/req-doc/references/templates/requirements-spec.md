# SRS需求规格说明书模板

> 本模板定义SRS需求规格说明书的完整文档结构，包含所有章节、表格格式、内容示例。
> req-writer 生成每个章节前必须对照本模板的内容要求和示例。

---

## 写作前必读：禁止词汇速查

生成任何内容前，确认不包含以下词汇：

| 禁止 | 替换为 |
|------|-------|
| 弹窗、Modal、Drawer、抽屉 | 弹出编辑窗口、弹出确认、右侧编辑面板 |
| 数据源：/api/xxx | 来源于[模块]中已启用的[数据] |
| 树形下拉、TreeSelect | 层级选择 |
| 开关组件、Switch | 开关、状态切换 |
| 回填、回显 | 自动填入已有数据 |
| 步骤条、Steps 组件 | 分步填写（共N步） |
| Tag 形式展示 | 逐个展示，支持移除 |
| hover 显示、tooltip | 悬停显示 |
| 调用接口、API 请求 | 提交、查询、获取 |
| 推送通知 | 通知、提醒 |
| 前端过滤、不请求接口 | 在已加载数据中过滤 |
| a-xxx、el-xxx、van-xxx | 禁止出现任何 UI 组件名 |
| flex、grid、margin、px | 禁止出现任何 CSS 属性 |

---

## 文档封面（YAML frontmatter）

```yaml
---
title: "SRS需求规格说明书"
subtitle: "[项目名称]"
author: "[客户单位名称]"
date: "编制单位：[编制单位名称]\n\n[年月中文，如：二零二六年四月]"
lang: zh-CN
toc: true
toc-depth: 3
numbersections: false
geometry: "left=3.17cm,right=3.17cm,top=2.54cm,bottom=2.54cm"
---
```

---

## 一、文档信息

| 项目名称 | [项目名称] |
|---------|-----------|
| 文档版本 | V1.0 |
| 编制日期 | [YYYY-MM-DD] |
| 编制人 | [编制人] |
| 审核人 | - |
| 批准人 | - |
| 客户单位 | [客户单位名称] |
| 编制单位 | [编制单位名称] |
| 适用范围 | [适用范围描述] |
| 核心目标 | [核心目标描述] |
| 文档状态 | 草稿 |

**历史版本**

| 版本 | 日期 | 作者 | 更改说明 |
|-----|------|------|---------|
| V1.0 | [YYYY-MM-DD] | [作者] | 初始版本 |

---

## 二、项目概述

### 2.1 项目背景

**内容要求：**
- 描述行业现状和面临的挑战，说明项目建设的必要性
- 列出3-5个具体问题点，每个问题点加粗标题 + 具体描述
- 字数：300-500字

**示例：**
```
随着[行业]的快速发展，[业务场景]面临以下挑战：

1. **[问题标题]**：[具体描述，说明问题的影响和后果]
2. **[问题标题]**：[具体描述]
3. **[问题标题]**：[具体描述]

为解决上述问题，建设[项目名称]，实现[核心价值]，提升[管理目标]。
```

### 2.2 项目建设目标

**内容要求：**
- 列出3-5个具体、可衡量的目标
- 每个目标：加粗名称 + 详细说明（包含功能点和业务价值）
- 字数：每个目标80-150字

**示例：**
```
本项目旨在建设一套[系统类型]，实现以下目标：

1. **[目标名称]**：[详细说明目标内容、实现方式和预期效果]
2. **[目标名称]**：[详细说明]
```

### 2.3 项目建设范围

**内容要求：**
- 明确系统端（PC端、移动端等）
- 列出功能模块清单
- 说明数据范围
- 明确不包含的功能（如有）

**示例：**
```
本项目建设范围包括：

**系统端**：
- PC端管理后台（Web应用）
- 移动端H5（支持微信/浏览器访问）

**功能模块**：
- 模块一：功能说明
- 模块二：功能说明

**数据范围**：
- 数据类型一：数据说明

**不包含的功能**：
- 功能一：说明原因（预留接口，后续迭代实现）
```

### 2.4 业务流程图

**必须调用 diagram-generator 技能生成**，参考模板：`examples/swimlane.xml`，图片命名：`images/business-flow.png`

### 2.5 业务角色定义

**内容要求：** 使用表格定义各类用户及其权限范围。

**示例：**
```
| 角色名称 | 角色描述 | 主要职责 | 权限范围 |
|---------|---------|---------|---------|
| 系统管理员 | 负责系统配置和用户管理 | 用户管理、权限配置 | 全部功能 |
| 业务主管 | 负责审批和监督 | 审批、查看统计 | 查看全部数据、审批操作 |
| 业务人员 | 日常业务处理 | 数据录入、查询 | 业务功能 |
```

---

## 三、项目建设内容

### 3.1 总体功能架构

**必须调用 diagram-generator 技能生成系统功能架构图**，图片命名：`images/system-architecture.png`

**多端架构说明（架构图下方补充）：**

若系统包含多个终端（PC 端、移动端等），必须分端描述菜单结构，不可混写。格式如下：

```
**PC 端（管理后台）**

- [一级菜单]（一级菜单）
  - [二级菜单]（二级菜单）

**移动端**

- [Tab 名称]（底部导航 Tab）
  - [子功能]
```

**单端菜单结构文字说明：**
- 只列出菜单中显示的功能，不列出详情页、编辑页等隐藏页面
- 标注层级：（一级菜单）、（二级菜单）
- 不标注权限说明（权限在 3.4 中统一说明）

**示例（单端）：**
```
- 工作台（一级菜单）
- 设备管理（一级菜单）
  - 设备档案（二级菜单）
  - 设备分类（二级菜单）
- 系统配置（一级菜单）
  - 用户管理（二级菜单）
  - 角色管理（二级菜单）
```

### 3.2 需求功能清单

**内容要求：**
- 从业务视角描述系统需要满足的需求，不涉及具体页面和功能点
- 优先级：P0（核心必须）、P1（重要）、P2（一般）
- 必须覆盖所有核心业务需求

**示例：**
```
| 序号 | 业务需求 | 需求概述 | 优先级 |
|-----|---------|---------|-------|
| 1 | 用户身份认证 | 系统用户通过账号密码登录，不同角色拥有不同功能权限 | P0 |
| 2 | 设备档案管理 | 统一维护设备基本信息，支持多维度查询，作为巡检和整改的基础数据 | P1 |
```

### 3.3 页面功能清单

**内容要求：**
- **多端系统必须分端列表**：PC 端和移动端分开写，不可混在一张表里
- 5列表格：**终端**、一级菜单、二级菜单、子功能、功能描述
  - 终端列：PC端 / 移动端（相同终端使用 `^` 合并）
  - 没有二级菜单时，二级菜单列填 `-`
  - 相同的一级菜单、二级菜单使用 `^` 合并单元格
- **子功能必须拆到最小操作粒度，每个操作独立一行**
  - PC 端标准操作集：筛选、列表、新增、编辑、删除、导出、导入、启用/停用、详情查看
  - 移动端标准操作集：查看列表、查看详情、提交、确认、上传照片、扫码等
  - 特有操作：审核、分配权限、连接测试、发布、撤回等，各自独立一行
- **功能描述列遵循 L1 概览层规则**（见 prd-language.md 内容分级规则）：
  - 格式：动词 + 对象 + 条件（可选）
  - 禁止出现图表视觉类型、颜色、尺寸、布局位置等视觉描述

**示例（多端）：**
```
| 终端 | 一级菜单 | 二级菜单 | 子功能 | 功能描述 |
|-----|---------|---------|-------|---------|
| PC端 | 设备管理 | 设备档案 | 筛选 | 按设备名称、分类、状态筛选 |
| ^ | ^ | ^ | 列表 | 分页展示设备列表，支持导出 |
| ^ | ^ | ^ | 新增 | 填写设备基本信息，上传图片和附件 |
| ^ | 报表中心 | 运单报表 | 指标统计 | 查看总运单数、总接卸量、完成率、单均接卸量四项指标 |
| ^ | ^ | ^ | 趋势分析 | 按时间维度查看接卸量变化趋势 |
| ^ | ^ | ^ | 占比分析 | 按油库或油品维度查看占比分布 |
| ^ | ^ | ^ | 排名对比 | 按发运量查看承运商和炼厂排名 |
| ^ | 系统配置 | 用户管理 | 列表 | 查询用户列表 |
| 移动端 | 工作台 | - | 创建运单 | 填写运单信息并提交 |
| ^ | ^ | - | 运单管理 | 查看进行中和已完成的运单列表 |
| ^ | 消息 | - | 通知列表 | 查看系统通知和业务提醒 |
```

### 3.4 页面访问权限

**示例：**
```
| 功能模块 | 系统管理员 | 业务主管 | 业务人员 |
|---------|-----------|---------|---------|
| 设备档案 | 全部权限 | 查看 | 全部权限 |
| 角色管理 | 全部权限 | 无 | 无 |
```

### 3.5 功能详细设计

**章节结构规范：**

- 3.3 中每个一级菜单对应 `### 3.5.x`
- 有二级菜单时：一级菜单写一段概述，二级菜单用 `#### 3.5.x.y`
- 无二级菜单时：一级菜单直接写功能设计内容
- **3.3 中列出的每个子功能，在此章节中必须有对应描述，不可遗漏**

---

**标准功能模块结构（无二级菜单）：**

```
### 3.5.1 [功能名称]

#### （一）功能需求概述

[1-3句话说明功能目的、使用场景和核心价值，不写实现方式]

#### （二）功能设计

[按3.3中的子功能逐一展开，见下方各子功能写法]

#### （三）业务逻辑关联

- [依赖关系：依赖[模块]提供[数据]，需先完成[模块]配置]
- [影响关系：本模块[状态/数据]变更后，影响[其他模块]的[具体行为]]

#### （四）用户操作流程图

必须调用 diagram-generator 技能生成，图片命名：`images/[功能名]-operation-flow.png`

#### （五）功能时序图

必须调用 diagram-generator 技能生成，图片命名：`images/[功能名]-sequence.png`
```

---

**标准功能模块结构（有二级菜单）：**

```
### 3.5.2 [一级菜单名称]

[一段概述，说明该一级菜单下各子模块的整体定位和价值]

#### 3.5.2.1 [二级菜单名称]

**（一）功能需求概述**

[1-3句话]

**（二）功能设计**

[按子功能逐一展开]

**（三）业务逻辑关联**

[依赖和影响关系]

**（四）用户操作流程图**

必须调用 diagram-generator 技能生成，图片命名：`images/[功能名]-operation-flow.png`

**（五）功能时序图**

必须调用 diagram-generator 技能生成，图片命名：`images/[功能名]-sequence.png`
```

---

**移动端 Tab 页面结构（底部导航 App）：**

移动端 App 以底部 Tab 导航为顶层结构，每个 Tab 对应一个独立章节。Tab 内的子功能作为 `**子功能名称**` 小标题展开，不再拆分为独立章节。

```
### 3.5.x 移动端

[2-3句话说明 App 整体定位、目标用户、底部导航结构]

---

#### 3.5.x.1 [Tab名称，如：首页]

**（一）功能需求概述**

[1-3句话说明该 Tab 的定位和核心价值]

**（二）功能设计**

**[子功能名称，如：待办事项]**

[说明 + 字段表格]

**[子功能名称，如：统计图表]**

[说明 + 字段表格]

**业务规则**

[触发条件 + 影响范围 + 提示文案原文，三要素]

**边界与异常**

[异常场景 + 处理方式 + 提示文案原文]

**（三）业务逻辑关联**

[跨模块依赖说明]

**（四）用户操作流程图**

![](images/mobile-[tab名]-operation-flow.png)

**（五）功能时序图**

![](images/mobile-[tab名]-sequence.png)

---

#### 3.5.x.2 [Tab名称，如：巡检]

...（结构同上）
```

**移动端章节规范：**
- Tab 数量即章节数量，有几个 Tab 就写几个 `#### 3.5.x.y`
- Tab 内的子功能（如"任务列表"、"导航巡检"）用 `**子功能名称**` 作为小标题，不拆独立章节
- 业务规则和边界与异常写在 Tab 章节末尾，统一覆盖该 Tab 下所有子功能
- 不写角色权限文案（"仅 XX 角色可见"等），权限由业务配置

---

**各子功能的标准写法：**

**查询/筛选功能**

```
支持以下条件筛选，条件之间为"与"关系，点击搜索按钮触发查询：

| 筛选字段 | 类型 | 说明 |
|---------|------|------|
| 设备名称 | 文本输入 | 支持模糊搜索 |
| 所属单位 | 层级选择 | 来源于组织管理中已启用的部门，支持按名称搜索 |
| 设备状态 | 下拉选择 | 正常/停用/维修中 |
| 创建时间 | 日期范围 | 选择开始和结束日期 |
```

**列表功能**

```
| 列名 | 字段类型 | 说明 |
|-----|---------|------|
| 设备名称 | 图片+文本 | 左侧缩略图，点击可预览大图；无图显示占位图 |
| 设备状态 | 标签 | 正常=绿色 / 停用=灰色 / 维修中=橙色 |
| 关联厂商 | 文本 | 无值显示"-" |
| 操作 | 操作按钮 | 编辑、删除（无权限时隐藏） |

分页默认每页10条，支持切换每页数量。
支持批量勾选，勾选后显示批量删除按钮（含已选数量）。
列表为空时显示：暂无[数据名]数据。
```

**新增功能**

```
点击"新增"进入新建[数据名]页面（或弹出编辑窗口）。

| 字段名称 | 类型 | 长度限制 | 必填 | 默认值 | 说明 |
|---------|------|---------|------|-------|------|
| 设备名称 | 文本输入 | 2-50字 | 是 | - | - |
| 设备编号 | 文本输入 | 2-30字 | 是 | - | 系统内唯一 |
| 设备分类 | 层级选择 | - | 是 | - | 来源于设备分类中已启用的分类 |
| 设备状态 | 下拉选择 | - | 是 | 正常 | 正常/停用/维修中 |
| 设备图片 | 图片上传 | 最多5张 | 否 | - | 单张最大10MB |

提交成功：[成功提示文案原文，如"新增成功"]。
提交失败：保留填写内容，显示具体错误原因。
```

**编辑功能**

```
点击"编辑"，自动填入当前记录数据，表单字段同新增。
提交成功：[成功提示文案原文，如"更新成功"]。
```

**删除功能**

```
点击"删除"，弹出确认"[确认文案原文]"。
删除成功：[成功提示文案原文]。
[如有关联保护：有关联数据时阻止删除，提示"[提示文案原文]"。]
```

**导出功能**

```
点击"导出"，将当前筛选结果导出为 Excel 文件。
导出成功：自动下载文件，提示"导出成功"。
```

**导入功能**

```
点击"导入"，弹出导入窗口。操作流程：下载模板 → 填写数据 → 上传文件 → 确认导入。
导入成功：提示"成功导入 X 条数据"，列表刷新。
导入失败：提示具体错误行号和错误原因。
```

**启用/停用功能**

```
点击状态开关直接切换，无需确认。
切换成功：提示"状态已更新"。
[如有跨模块影响：停用后，[其他模块]中[具体影响]。]
```

**状态流转**（仅有明确状态机的功能使用，如工单、任务、审批流）

```
| 当前状态 | 触发条件 | 流转至 | 系统动作 |
|---------|---------|-------|---------|
| 待审核 | 审核人点击"审核通过"并指派整改人 | 待整改 | 通知整改人员："您有新的整改任务待处理" |
| 待审核 | 审核人点击"退回" | 已关闭 | 通知提交人："您的工单已被退回，原因：[退回原因]" |
```

**业务规则**（每条规则必须包含三要素：触发条件 + 影响范围 + 提示文案原文）

```
- [字段名]在系统内全局唯一。新增/编辑时若重复，阻止提交，提示"[字段名]【XXX】已存在，请更换"。
- 删除[数据名]时，若存在关联[数据名]，阻止删除，提示"该[数据名]下有 X 条关联数据，请先解除关联"。
- [状态]变更为[值]后，[跨模块影响的具体业务描述]。
```

**边界与异常**

```
- **空数据**：列表无数据时，显示"暂无[数据名]数据"
- **权限不足**：[操作]按钮对无权限角色完全隐藏（不渲染，非禁用）
- **[其他异常场景]**：[处理方式和提示文案原文]
```

---

## 四、技术方案

### 4.1 技术架构

**内容要求：** 前端技术栈、后端技术栈、数据库选型、部署架构。只写技术方案和思路，不写代码。

### 4.2 数据交互

**内容要求：** 前后端数据交互方式、API 设计原则、数据格式规范。

### 4.3 外部系统集成（可选）

**如无外部系统集成，可省略此章节。**

### 4.4 安全方案

**内容要求：** 身份认证、权限控制、数据加密、安全审计。

---

## 五、非功能需求

### 5.1 性能需求

- 响应时间：页面加载时间 < 3秒，接口响应时间 < 1秒
- 并发用户数：支持 XXX 并发用户
- 数据处理能力：每秒处理 XXX 条数据

### 5.2 安全需求

- 身份认证：账号密码登录，支持多因素认证
- 数据安全：敏感数据加密存储，传输使用 HTTPS
- 权限控制：基于角色的访问控制（RBAC）

### 5.3 可用性需求

- 系统可用性：99.9%
- 故障恢复：RTO < 1小时，RPO < 15分钟
- 数据备份：每日自动备份

### 5.4 兼容性需求

- 浏览器：Chrome、Firefox、Safari、Edge 最新版本
- 设备：PC、平板、手机
- 分辨率：1920×1080、1366×768、移动端自适应

---

## 六、数据需求

### 6.1 数据字典

**内容要求：** 枚举值定义，使用表格展示。

**示例：**
```
| 枚举类型 | 枚举值 | 说明 |
|---------|-------|------|
| 设备状态 | 正常 | 设备运行正常 |
| ^ | 停用 | 设备已停用，不可被巡检计划选择 |
| ^ | 维修中 | 设备维修中 |
| 工单状态 | 待审核 | 工单已提交，等待审核 |
| ^ | 待整改 | 审核通过，等待整改人员处理 |
| ^ | 已完成 | 整改完成并通过复核 |
```

### 6.2 数据存储要求

- 数据保留期限：XXX年
- 存储容量规划：预计 XXX GB
- 数据归档策略：超过 XXX 年的数据归档至冷存储

---

## 七、接口需求（可选）

### 7.1 第三方系统接入接口规范

**如无第三方系统接入，可省略此章节。**

---

## 八、项目实施与验收

### 8.1 实施阶段

1. **需求确认阶段**（X周）：需求评审、原型确认、需求基线锁定
2. **设计开发阶段**（X周）：详细设计、编码开发、单元测试
3. **测试部署阶段**（X周）：功能测试、性能测试、用户验收测试、部署上线
4. **上线运行阶段**（X周）：试运行、问题修复、正式上线

### 8.2 系统培训服务

- **培训对象**：系统管理员、业务人员
- **培训内容**：系统操作、功能使用、常见问题处理
- **培训方式**：现场培训、在线培训、操作手册

### 8.3 验收标准

- **功能验收**：所有功能按SRS需求规格说明书实现，无P0/P1级缺陷
- **性能验收**：满足第五章非功能需求指标
- **文档验收**：提供完整的技术文档和操作手册

---

## 九、附录

### 附录A：术语表

| 术语 | 说明 |
|-----|------|
| RBAC | 基于角色的访问控制（Role-Based Access Control） |
| RTO | 恢复时间目标（Recovery Time Objective） |
| RPO | 恢复点目标（Recovery Point Objective） |
| SRS | 软件需求规格说明书（Software Requirements Specification） |

### 附录B：参考资料

- [相关标准文档]
- [参考资料]

---

**模板说明：**
- 本模板定义了SRS需求规格说明书的完整结构
- 生成文档时，根据实际项目情况调整章节内容
- 六、七、九章节可根据项目实际情况简化或省略
- 所有图表必须通过 diagram-generator 技能生成
- 每次生成内容前必须对照顶部"禁止词汇速查"检查
