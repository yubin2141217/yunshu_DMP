---
name: pm-product-pipeline
description: "产品 AI Agent 全流程编排技能。将「市场调研 → 用户画像 → 功能优先级 → 产品路线图 → 需求说明书 → 前端原型 → 测试用例 → 操作手册 → 发版说明」九个阶段串联成一个完整的智能流水线。适用场景：(1) 用户想一次性走完产品从0到1的完整交付流程，(2) 用户说「走完整流程」「一键生成所有文档」「完整产品交付」时触发，(3) 用户提供一个产品想法，希望输出所有相关文档和原型"
---

# 产品 AI Agent 全流程编排

## 流程概览

### 完整流程（九个阶段）

```
用户输入想法
    ↓
[阶段0] 市场调研         → docs/market-research.md
    ↓
[阶段1] 用户画像         → docs/user-persona.md
    ↓
[阶段2] 功能优先级       → docs/feature-priority.md
    ↓
[阶段3] 产品路线图       → docs/roadmap.md
    ↓
[阶段4] 需求澄清         → docs/requirements.md
    ↓
[阶段5] PRD 撰写         → docs/*PRD*.md（`prd-writer`）
    ↓
[阶段5b] 页面结构整理    → docs/*功能清单*.md（`prototype-list`，可选）
    ↓
[阶段6] 静态原型         → web/ / admin/ / app/（`prototype-demo`）
    ↓
[阶段7] 测试用例         → docs/*-测试用例-*.md
    ↓
[阶段8] 操作手册         → docs/*-操作手册-*.md
    ↓
[阶段9] 发版说明         → docs/release-notes.md
```

### 快速流程（五个阶段）

如果用户已有明确需求，可跳过前置阶段：

```
需求澄清 → PRD（prd-writer）→ 静态原型（generator）→ 测试用例 → 操作手册
```

每个阶段**读取上一阶段的输出**，保证内容一致性和上下文传递。

---

## 启动流程

### Step 0: 初始化 — 了解全局信息

收到用户请求后，**首先**用 AskUserQuestion 一次性收集关键决策信息：

**问题1**：产品类型与交付端是什么？
- WEB 静态站 / 企业官网 / 落地页（`prototype-demo` → `web/`）
- 管理后台 / CRUD（`prototype-demo` → `admin/`）
- APP / 小程序壳内原型（`prototype-demo` → `app/`）
- 两端都要（先确认优先级）
- 其他（请描述）

**问题2**：想走哪些阶段？
- 完整流程（市场调研 → 发版说明，共10个阶段）
- 快速流程（需求澄清 → 操作手册，共5个阶段）
- 只要文档（跳过静态原型生成）
- 自定义选择

**问题3**：现在手头有什么？
- 只有一个想法/描述（从市场调研开始）
- 已有市场调研报告（从用户画像开始）
- 已有明确需求（从需求澄清开始）
- 已有 PRD（从 `prototype-list` 或原型生成开始）

收集完成后，立即用 TaskCreate 创建任务清单。

---

## 任务清单创建规范

根据用户选择的阶段，创建对应任务。示例（完整流程）：

```
TaskCreate: 阶段0 - 市场调研
TaskCreate: 阶段1 - 用户画像
TaskCreate: 阶段2 - 功能优先级排序
TaskCreate: 阶段3 - 产品路线图
TaskCreate: 阶段4 - 需求澄清
TaskCreate: 阶段5 - PRD 撰写（prd-writer）
TaskCreate: 阶段5b - 页面结构整理（prototype-list，可选）
TaskCreate: 阶段6 - 静态原型：[页面A]
TaskCreate: 阶段6 - 静态原型：[页面B]
TaskCreate: 阶段7 - 测试用例
TaskCreate: 阶段8 - 操作手册
TaskCreate: 阶段9 - 发版说明
```

---

## 阶段0：市场调研

**目标**：分析市场规模、竞品情况、行业趋势，验证产品机会

**执行方式**：调用 `pm-market-research` skill

**必须调研的内容**：
1. 市场规模分析（TAM/SAM/SOM）
2. 目标用户群体特征
3. 竞品分析（3-5个主要竞品）
4. 行业趋势和机会点
5. 差异化定位建议

**输出**：`docs/market-research.md`

```markdown
# [产品名称] 市场调研报告

## 市场规模
- TAM（总体市场）：[规模估算]
- SAM（可服务市场）：[规模估算]
- SOM（可获得市场）：[规模估算]

## 目标用户
[用户群体特征、痛点、需求]

## 竞品分析
[竞品对比表格]

## 行业趋势
[趋势分析]

## 差异化定位
[建议的差异化方向]
```

**完成标志**：市场调研报告已写入 `docs/market-research.md`

---

## 阶段1：用户画像

**目标**：基于市场调研，创建详细的用户画像

**输入**：`docs/market-research.md`（阶段0输出）

**执行方式**：调用 `pm-user-persona` skill

**传入上下文**：
```
请读取 docs/market-research.md，基于目标用户群体创建用户画像。

要求：
- 创建 2-3 个典型用户画像
- 每个画像包含：基本信息、目标、痛点、使用场景
- 绘制用户旅程地图
```

**输出**：`docs/user-persona.md`

**完成标志**：用户画像文档已写入，包含至少2个典型用户画像

---

## 阶段2：功能优先级

**目标**：基于用户画像，对功能需求进行优先级排序

**输入**：
- `docs/market-research.md`（阶段0输出）
- `docs/user-persona.md`（阶段1输出）

**执行方式**：调用 `pm-feature-prioritization` skill

**传入上下文**：
```
请读取 docs/market-research.md 和 docs/user-persona.md，
对以下功能需求进行优先级排序：

[用户提供的功能列表]

使用 RICE 模型评估，输出：
- P0（必须有）功能列表
- P1（重要）功能列表
- P2（可选）功能列表
```

**输出**：`docs/feature-priority.md`

**完成标志**：功能优先级文档已写入，所有功能已分级

---

## 阶段3：产品路线图

**目标**：基于功能优先级，规划产品迭代路线图

**输入**：
- `docs/feature-priority.md`（阶段2输出）
- `docs/user-persona.md`（阶段1输出）

**执行方式**：调用 `pm-roadmap` skill

**传入上下文**：
```
请读取 docs/feature-priority.md，规划产品路线图。

要求：
- 将功能分配到 3-4 个版本
- V1.0 只包含 P0 功能（MVP）
- 每个版本设定里程碑和交付时间
- 说明版本间的依赖关系
```

**输出**：`docs/roadmap.md`

**完成标志**：产品路线图已写入，V1.0 功能范围明确

---

## 阶段4：需求澄清

**目标**：把用户的模糊想法转化为结构化的需求描述

**执行方式**：通过对话逐步澄清，不一次性抛出所有问题

**必须澄清的内容**：

```markdown
1. 产品是什么？解决谁的什么问题？
2. 目标用户是谁？（角色类型、使用场景）
3. 核心功能有哪些？（让用户列举，而不是猜）
4. 涉及哪些端？（Web管理后台 / 用户端 / 移动端）
5. 有没有特殊的业务规则或约束？
```

**输出**：`docs/requirements.md`

```markdown
# [产品名称] 需求概述

## 产品定位
[一句话描述]

## 目标用户
[用户角色列表]

## 核心功能清单
[功能列表，按模块组织]

## 涉及端
- [ ] Web 管理后台
- [ ] Web 用户端
- [ ] 移动端 H5

## 特殊说明
[业务规则、约束条件]
```

**完成标志**：用户确认需求描述准确，且已写入 `docs/requirements.md`

---

## 阶段5：PRD 撰写

**目标**：将需求概述扩展为结构化的产品需求文档（PRD）

**输入**：
- `docs/requirements.md`（阶段4输出）
- `docs/roadmap.md`（阶段3输出，了解 V1.0 范围）

**执行方式**：调用 `prd-writer` skill，传入阶段4的需求描述

**关键规范**：
- 读取 `docs/requirements.md` 和 `docs/roadmap.md` 作为输入上下文
- 只覆盖 V1.0 版本的功能（根据路线图）
- 严格遵循 `prd-writer` 的第零步、三视角诊断与分阶段交付
- **不写实现级代码细节**，与 generator 分工

**输出**：`docs/YYYY-MM-DD-<主题>-PRD.md`

**完成标志**：PRD 覆盖所有 V1.0 功能，用户确认内容准确

---

## 阶段5b：页面结构整理（可选）

**目标**：将 PRD 提炼为功能清单 + 完整页面结构，便于落原型

**输入**：`docs/*PRD*.md`（阶段5输出）

**执行方式**：调用 `prototype-list` skill

**输出**：`docs/YYYY-MM-DD-<主题>功能清单.md`

**完成标志**：第 2 章为完整页面结构列表（按屏分段、每屏五项要点）

---

## 阶段6：静态原型

**目标**：基于 PRD 与页面结构生成可预览的静态原型

**输入**：
- `docs/*PRD*.md`（阶段5输出）
- `docs/*功能清单*.md`（阶段5b输出，如有）

**执行方式**：

### Step 6.1 — 选定视觉规范

- 用户已指定 → 读取 `.style/*-style.md`
- 用户未指定 → 询问或推荐 `.style` 预置规范
- 需对齐外部站点 → 可先调用 `style-extractor`

### Step 6.2 — 逐页面生成

根据产品类型选择 generator：
- WEB 静态页 → `prototype-demo`（产出在 `web/`）
- 管理后台 → `prototype-demo`（产出在 `admin/`）
- APP / 小程序壳内原型 → `prototype-demo`（产出在 `app/`）

为 `docs/` 下功能清单或 PRD 中的每个页面/屏创建独立任务，逐页生成 HTML/CSS/JS 分离文件。

**每生成一个页面**，在对应 TaskCreate 任务中标记 completed。

**输出**：`web/` 或 `app/` 下可离线预览的静态原型

**完成标志**：V1.0 范围内所有页面/屏已生成，可在浏览器中直接打开预览

---

## 阶段7：测试用例

**目标**：基于需求说明书生成完整的测试用例集

**输入**：`docs/*PRD*.md`（阶段5输出）、`docs/*功能清单*.md`（如有）

**执行方式**：调用 `pm-test-cases` skill

**传入上下文**：
```
请读取 docs/ 下 PRD 与功能清单，为以下功能模块生成测试用例：
[列出所有功能模块名称]

要求：
- 覆盖功能测试、边界测试、异常测试、权限测试
- P0 用例覆盖所有主流程
- 每个必填字段都有为空的测试用例
- 每个有权限控制的功能都有权限越权测试
```

**输出**：`docs/test-cases.md`

**完成标志**：所有功能模块都有测试用例，P0 冒烟测试集不少于核心流程数量

---

## 阶段8：操作手册

**目标**：基于需求说明书和原型，生成面向用户的操作手册

**输入**：
- `docs/*PRD*.md`（阶段5输出）
- `web/` 或 `app/`（阶段6输出，了解实际页面结构）

**执行方式**：调用 `pm-operation-manual` skill

**默认生成两个版本**：
1. 用户操作手册（`docs/operation-manual-user.md`）：面向终端用户
2. 快速入门指南（`docs/quick-start.md`）：新用户10分钟上手

如有管理员角色，额外生成管理员手册（`docs/operation-manual-admin.md`）

**传入上下文**：
```
请读取 docs/ 下 PRD，结合 web/ 或 app/ 原型页面，生成用户操作手册。

系统信息：
- 系统名称：[从 PRD 提取]
- 用户角色：[从 PRD 提取]
- 主要功能模块：[从 PRD / 功能清单 提取]

要求：
- 操作步骤清晰，每步一个动作
- 界面元素用【】标注
- 关键注意事项用"注意："标注
```

**完成标志**：手册覆盖所有核心功能，操作步骤可独立执行

---

## 阶段9：发版说明

**目标**：基于需求说明书和实际交付内容，生成发版说明文档

**输入**：
- `docs/*PRD*.md`（阶段5输出）
- `docs/roadmap.md`（阶段3输出，了解版本规划）
- `web/` 或 `app/`（阶段6输出，了解实际交付的页面）

**执行方式**：调用 `pm-release-notes` skill

**传入上下文**：
```
请读取 docs/ 下 PRD 和 docs/roadmap.md，生成 V1.0 版本的发版说明。

版本信息：
- 版本号：V1.0
- 发布日期：[预计发布日期]
- 主要功能：[从 PRD 提取的 V1.0 功能列表]

要求：
- 面向用户的语言，突出功能价值
- 按功能模块分类说明
- 包含已知问题和后续计划
```

**输出**：`docs/release-notes.md`

**完成标志**：发版说明清晰描述所有新功能，用户可快速了解版本更新内容

---

## 流程控制规则

### 阶段间传递规范

每个阶段完成后，必须确保输出文件已写入，才能进入下一阶段：

```
阶段0完成 → docs/market-research.md ✅ → 进入阶段1
阶段1完成 → docs/user-persona.md ✅ → 进入阶段2
阶段2完成 → docs/feature-priority.md ✅ → 进入阶段3
阶段3完成 → docs/roadmap.md ✅ → 进入阶段4
阶段4完成 → docs/requirements.md ✅ → 进入阶段5
阶段5完成 → docs/*PRD*.md ✅ → 进入阶段5b/6/7/8
阶段5b完成 → docs/*功能清单*.md ✅（可选）
阶段6完成 → web/ 或 app/ ✅ → 进入阶段8（手册需要了解原型结构）
阶段7完成 → docs/test-cases.md ✅
阶段8完成 → docs/operation-manual*.md ✅ → 进入阶段9
阶段9完成 → docs/release-notes.md ✅
```

### 可以并行的阶段

阶段5（PRD）完成后，阶段6/7/8**可以并行**：
- 测试用例主要依赖 PRD 与功能清单，不依赖原型页面
- 操作手册主要依赖 PRD，可以先生成再补充原型细节

### 用户确认节点

以下节点**必须等用户确认**后再进入下一阶段：
1. 阶段0结束后：用户确认市场调研结论
2. 阶段3结束后：用户确认产品路线图
3. 阶段4结束后：用户确认需求描述准确
4. 阶段5结束后：用户确认 PRD 符合预期（可修改）
5. 阶段6每个页面生成后：用户确认页面结构正确

### 进度汇报

每完成一个阶段，输出进度摘要：

```
✅ 阶段[N] 完成

产出文件：[文件路径]
核心内容：[2-3句话描述产出内容]
下一步：[说明下一阶段将做什么，需要用户确认什么]

---
整体进度：[N]/10 个阶段完成
```

---

## 目录结构规范

流程结束后，项目目录结构如下：

```
[项目目录]/
├── docs/
│   ├── market-research.md          ← 阶段0：市场调研
│   ├── user-persona.md             ← 阶段1：用户画像
│   ├── feature-priority.md         ← 阶段2：功能优先级
│   ├── roadmap.md                  ← 阶段3：产品路线图
│   ├── requirements.md             ← 阶段4：需求概述
│   ├── YYYY-MM-DD-<主题>-PRD.md     ← 阶段5：PRD（prd-writer）
│   ├── YYYY-MM-DD-<主题>功能清单.md ← 阶段5b：功能与页面结构（prototype-list，可选）
│   ├── *-测试用例-*.md             ← 阶段7：测试用例
│   ├── *-操作手册-*.md             ← 阶段8：操作手册
│   └── release-notes.md            ← 阶段9：发版说明
├── web/                            ← 阶段6：WEB 静态原型（prototype-demo）
│   ├── index.html
│   ├── css/
│   └── js/
├── admin/                          ← 阶段6：管理后台原型（prototype-demo，按需）
└── app/                            ← 阶段6：APP 壳内原型（prototype-demo，按需）
```

---

## 快速恢复（断点续跑）

如果中途中断，再次启动时：

1. 检查 `docs/` 目录已有哪些文件
2. 根据已有文件判断完成到哪个阶段
3. 从未完成的阶段继续，无需重新开始

判断逻辑：
```
docs/market-research.md 存在     → 阶段0已完成
docs/user-persona.md 存在        → 阶段1已完成
docs/feature-priority.md 存在    → 阶段2已完成
docs/roadmap.md 存在             → 阶段3已完成
docs/requirements.md 存在        → 阶段4已完成
docs/*PRD*.md 存在               → 阶段5已完成
docs/*功能清单*.md 存在            → 阶段5b已完成（或跳过）
web/ 或 app/ 有页面文件           → 阶段6已完成（或进行中）
docs/*-测试用例-*.md 存在        → 阶段7已完成
docs/operation-manual*.md 存在   → 阶段8已完成
docs/release-notes.md 存在       → 阶段9已完成
```
