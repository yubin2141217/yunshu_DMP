# 概要设计说明书（HLD）模板

> 本模板定义概要设计说明书的完整文档结构。design-writer 生成每个章节前必须对照本模板的内容要求和示例。
> 内容规范见 `.agents/knowledge/phase2-design/hld-spec.md`。

---

## 写作前必读：分层边界

概要设计写到"模块 / 实体 / 接口分组"级即止。出现以下内容 = 越界，应下沉到详细设计（LLD）：

| 越界内容（属于 LLD） | HLD 正确写法 |
|--------------------|------------|
| 字段级表结构（VARCHAR/INT...） | 业务实体 + 实体关系 |
| 单接口请求/响应/错误码 | 接口分组 + 协议 |
| 类方法实现、代码 | 模块职责 |

技术词汇（Vue/MySQL/Redis/REST/JWT/分层/缓存）在 HLD 中**必须使用**，不要回避——这与需求文档相反。

---

## 文档封面（YAML frontmatter）

```yaml
---
title: "概要设计说明书"
subtitle: "[项目名称]"
author: "[客户单位名称]"
date: "编制单位：[编制单位名称]\n\n[年月中文，如：二零二六年五月]"
lang: zh-CN
toc: true
toc-depth: 3
numbersections: false
geometry: "left=3.17cm,right=3.17cm,top=2.54cm,bottom=2.54cm"
---
```

---

## 一、文档信息

| 项目名称 | [项目名称] |
|---------|-----------|
| 文档版本 | V1.0 |
| 编制日期 | [YYYY-MM-DD] |
| 编制人 | [编制人] |
| 关联需求文档 | [SRS需求规格说明书文件名] |
| 文档状态 | 草稿 |

**历史版本**

| 版本 | 日期 | 作者 | 更改说明 |
|-----|------|------|---------|
| V1.0 | [YYYY-MM-DD] | [作者] | 初始版本 |

---

## 二、引言

### 2.1 编写目的
[说明本概要设计的目的、预期读者（架构师/技术负责人/开发组长）]

### 2.2 设计依据
[列出依据的 SRS 需求文档、行业标准、技术约束]

### 2.3 术语与缩略语
| 术语 | 说明 |
|-----|------|
| RBAC | 基于角色的访问控制 |

---

## 三、系统总体设计

### 3.1 系统定位
[一句话说清系统是什么 + 目标用户 + 核心业务能力]

### 3.2 系统边界
**内容要求：** 本系统负责什么、不负责什么、与哪些外部系统交互。

| 边界项 | 说明 |
|-------|------|
| 系统职责 | [本系统提供的核心能力] |
| 不在范围 | [明确不做的事] |
| 外部系统 | [对接的外部系统及交互内容] |

### 3.3 系统总体架构图
**必须调用 diagram-generator 技能生成**（DIAGRAM_TYPE: architecture），图片命名：`images/hld-system-architecture.png`

[图下方用文字解读关键节点]

---

## 四、应用架构

### 4.1 分层设计
**内容要求：** 按实际裁剪分层（表现层/网关层/应用服务层/领域层/数据访问层/数据层），每层职责 + 关键技术。

| 分层 | 职责 | 关键技术 |
|------|------|---------|
| 表现层 | 用户交互、页面渲染 | Vue 3 + Element Plus |
| 网关层 | 路由转发、统一鉴权、限流 | [网关] |
| 应用服务层 | 业务编排、事务控制 | [后端框架] |
| 数据访问层 | 数据持久化 | [ORM] |
| 数据层 | 数据存储 | MySQL / Redis |

### 4.2 功能模块划分
**内容要求：** 模块清单必须保留"对应SRS功能"列体现可追溯性。

| 模块 | 职责 | 对应SRS功能 | 依赖模块 |
|-----|------|-----------|---------|
| [模块名] | [职责] | 3.5.x | [依赖的模块] |

**模块划分依据：** [按业务域 / 按技术分层，说明理由]

### 4.3 模块依赖关系
**必须调用 diagram-generator 技能生成模块关系图**（DIAGRAM_TYPE: architecture），图片命名：`images/hld-module-relation.png`

[说明依赖方向，循环依赖需说明理由]

---

## 五、数据架构

### 5.1 核心业务实体
**内容要求：** 列出核心业务实体（名词，如"运单""设备"，不是表，不含字段）。

| 实体 | 业务含义 | 关联实体 | 关系类型 |
|-----|---------|---------|---------|
| [实体名] | [含义] | [关联的实体] | 1:N / N:M |

### 5.2 概念实体关系图
**必须调用 diagram-generator 技能生成**（DIAGRAM_TYPE: er，概念级不含字段），图片命名：`images/hld-concept-er.png`

### 5.3 存储选型
| 存储类型 | 选型 | 承载数据 | 理由 |
|---------|------|---------|------|
| 关系数据库 | MySQL 8.0 | 业务主数据 | [理由] |
| 缓存 | Redis | 会话、热点数据 | [理由] |
| 对象存储 | [选型] | 图片、附件 | [理由] |

---

## 六、接口架构

### 6.1 接口分组
**内容要求：** 按模块分组，区分对外/对内接口边界，不下沉到单接口。

| 接口分组 | 所属模块 | 对外/对内 | 协议 |
|---------|---------|----------|------|
| [分组名] | [模块] | 对外 | REST |

### 6.2 接口协议
[REST / WebSocket / 消息队列 及各自适用场景]

### 6.3 第三方集成
| 外部系统 | 集成方式 | 用途 |
|---------|---------|------|
| [短信/地图/支付] | [API/SDK] | [用途] |

---

## 七、技术架构

**内容要求：** 每项技术选型给出理由。

| 层 | 技术选型 | 选型理由 |
|----|---------|---------|
| 前端 | [技术栈] | [理由] |
| 后端 | [技术栈] | [理由] |
| 数据库 | [选型] | [理由] |
| 中间件 | [缓存/消息/网关] | [理由] |
| 鉴权 | [JWT/Session] | [理由] |
| 文件存储 | [选型] | [理由] |

---

## 八、部署架构

### 8.1 部署形态
[单体 / 前后端分离 / 微服务，环境划分：开发/测试/生产]

### 8.2 部署架构图（可选）
**如需，调用 diagram-generator 技能生成**（DIAGRAM_TYPE: architecture），图片命名：`images/hld-deployment.png`

### 8.3 高可用与扩展策略
[负载均衡、多实例、健康检查等，如适用]

---

## 九、非功能性设计

**内容要求：** 将 SRS 第五章非功能需求逐条映射到架构应对策略。

| SRS非功能需求 | 指标 | 架构应对策略 |
|-------------|------|------------|
| 性能 | 接口响应<1s | 索引优化 + Redis缓存 + 分页 |
| 可用性 | 99.9% | 多实例部署 + 健康检查 |
| 安全 | RBAC | 网关层统一鉴权 + 权限码控制 |
| 兼容性 | 主流浏览器 | [策略] |

---

## 十、公共机制设计（可选）

[统一鉴权、统一异常处理、日志规范、缓存策略、文件上传等贯穿全系统的公共机制，架构级描述]

---

**模板说明：**
- 本模板定义概要设计说明书的完整结构，按实际项目裁剪
- 所有图表必须通过 diagram-generator 技能生成
- 写作前对照顶部"分层边界"，不要写入属于 LLD 的实现级细节
- 每个功能模块必须标注对应 SRS 功能编号
- 章节编号使用阿拉伯数字（一、二…用于大章，3.1/3.2 用于子节）
